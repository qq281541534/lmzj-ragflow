> 关联 Issue（必填，禁用 Closes/Fixes/Resolves 自动关闭）：
> Refs #

### What problem does this PR solve?

_Briefly describe what this PR aims to solve. Include background context that will help reviewers understand the purpose of the PR._

### Type of change

- [ ] Bug Fix (non-breaking change which fixes an issue)
- [ ] New Feature (non-breaking change which adds functionality)
- [ ] Documentation Update
- [ ] Refactoring
- [ ] Performance Improvement
- [ ] Other (please describe):

### 验证 / Verification

_列出运行的验证命令与结果（如 `ruff check`、`uv run pytest`、`npm run lint`、手动验证）。_

### 部署影响 / Deployment impact

_是否影响 runtime / 镜像构建？新增的 Secrets / Variables？数据库迁移？compose / manifest 变更？无则写「无」。_

### 回滚 / Rollback

_如何回滚本次变更（回退到上一版不可变 SHA 镜像或 revert 提交）。_
